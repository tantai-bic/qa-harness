{
  "$schema": "https://json.schemastore.org/claude-code-plugin.json",
  "name": "session",
  "displayName": "Session Lifecycle & Logging",
  "description": "Session lifecycle hooks: session-start (preload state + init telemetry), session-stop (flush logs), session-cleanup (SessionEnd), session-logger-init (UserPromptSubmit begin), session-logger-tool (PostToolUse). Bundles langfuse-helper.js for telemetry traceability (self-contained — no cross-plugin dep).",
  "version": "1.0.0",
  "author": {
    "name": "TanTai-BIC"
  },
  "keywords": [
    "session",
    "logging",
    "telemetry",
    "lifecycle",
    "hooks"
  ],
  "components": {
    "hooks": "./hooks"
  }
}
